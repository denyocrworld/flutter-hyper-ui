import 'package:flutter/material.dart';

class HttpTutorialView extends StatelessWidget {
  const HttpTutorialView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
